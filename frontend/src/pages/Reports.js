import React from "react";
import { Typography, Box } from "@mui/material";

const Reports = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Reports
      </Typography>
      <Typography variant="body1">View and generate reports here</Typography>
    </Box>
  );
};

export default Reports;
