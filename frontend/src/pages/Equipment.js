import React from "react";
import { Typography, Box } from "@mui/material";

const Equipment = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Equipment
      </Typography>
      <Typography variant="body1">Manage laboratory equipment here</Typography>
    </Box>
  );
};

export default Equipment;
